# Локально (npm run dev): прокси /api на backend
VITE_API_PROXY=http://127.0.0.1:8080

# Публичный URL сайта для canonical и Open Graph (продакшен)
# VITE_SITE_URL=https://pan-tech.ru

# В Docker через nginx /api проксируется автоматически.
