services:
  backend:
    environment:
      UVICORN_RELOAD: "1"
      RUN_SEED: "1"
    volumes:
      - ./backend:/app
    ports:
      - "${FASTAPI_PORT:-8080}:${FASTAPI_PORT:-8080}"

  db:
    ports:
      - "${POSTGRES_PORT:-5432}:5432"
